{
  "version": 2,
  "builds": [
    {
      "src": "api/build.sh",
      "use": "@vercel/static-build",
      "config": { "distDir": "api" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "api/api.py" }
  ]
}
